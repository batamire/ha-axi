// Reported by `ha-axi --version`. Kept in sync with package.json by
// release-please through the `x-release-please-version` annotation on the line
// below (see `extra-files` in release-please-config.json). Without that
// annotation release-please does not touch this file and the CLI silently
// reports a stale version after each release.
export const VERSION = "0.1.0"; // x-release-please-version
